import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#64ECC7',
    secondary: '#fff',
    loginColor: '#64ECC7',

    error: '#f13a59'
  },
};
