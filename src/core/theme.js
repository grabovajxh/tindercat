import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#ddd',
    secondary: '#fff',
    loginColor: '#ddd',
    logoColor:'#DA8730',


    error: '#f13a59'
  },
};
